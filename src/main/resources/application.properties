spring.application.name=algorithms
# Reject JSON numbers with decimals (e.g. 3.14) when deserializing to integer types.
# Default Jackson behavior silently truncates 3.14 -> 3, which would mask bad input.
spring.jackson.deserialization.accept-float-as-int=false
